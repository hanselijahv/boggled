module Boggled {
    interface Client {
        void login(in string username, in string password);
        void logout(in string username);
        void joinGame(in string username, in string gameid);
        void sendWords(in string username, in sequence<string> words);
        long getRoundScore(in string username, in string gameid);

    };
};