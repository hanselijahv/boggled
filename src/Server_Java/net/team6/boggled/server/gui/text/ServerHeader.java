package Server_Java.net.team6.boggled.server.gui.text;

public class ServerHeader extends ServerText {
    public ServerHeader(String text, int fontSize) {
        super(text, fontSize);
        dropShadowOffset = 4;
    }
}
